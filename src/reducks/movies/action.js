export const FETCH_MOVIES = "FETCH_MOVIES"
export const fetchMoviesAction = (movies) => {
    return {
        type: "FETCH_MOVIES",
        payload: {movies}
    }
}

// arrow funcitons are anonymous

// () => {}

// named export

// named and default

